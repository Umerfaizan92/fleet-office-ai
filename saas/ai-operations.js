(()=>{
  if(!location.pathname.includes('/saas/workspace'))return;
  const $=s=>document.querySelector(s);
  const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const VERSION='18.0';
  const localeMap={en:'en-AU',ur:'ur-PK',hi:'hi-IN',pa:'pa-IN',ar:'ar-SA',zh:'zh-CN',ja:'ja-JP',ko:'ko-KR',bn:'bn-BD',ta:'ta-IN',es:'es-ES',fr:'fr-FR'};
  const femaleHints=/female|heera|sana|samantha|victoria|aria|jenny|zira|hazel|karen|tessa|susan|sonia|natasha|veena|ava|emma|olivia|neerja|shimmer|nova|marin/i;
  const maleHints=/male|asad|david|mark|guy|ryan|george|daniel|james|ravi|hemant|imran|liam|aaron|onyx|cedar/i;
  const speechSupported='speechSynthesis' in window&&'SpeechSynthesisUtterance' in window;
  const Recognition=window.SpeechRecognition||window.webkitSpeechRecognition;
  let recognition=null,listening=false,voiceReplies=localStorage.getItem('superpro_workspace_voice')!=='off',lastLanguage='en',lastTopic='',speechRun=0,heartbeat=null,voices=[],serverAudio=null,serverAudioUrl='',mediaRecorder=null,mediaStream=null;

  function installStyle(){
    if($('#spai-v16-style'))return;
    const s=document.createElement('style');s.id='spai-v16-style';s.textContent=`
    .spai-v16{margin:18px 0 22px;border:1px solid var(--line);border-radius:18px;background:linear-gradient(180deg,color-mix(in srgb,var(--panel) 96%,#d9ad59 4%),var(--panel));box-shadow:var(--shadow-sm);overflow:hidden}
    .spai-head{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:18px 20px;border-bottom:1px solid var(--line)}
    .spai-head h2{margin:0;font-size:16px}.spai-head p{margin:4px 0 0;color:var(--muted);font-size:10px;line-height:1.5}.spai-head-actions{display:flex;gap:7px;align-items:center;flex-wrap:wrap;justify-content:flex-end}.spai-head select,.spai-head button{min-height:36px;border:1px solid var(--line);border-radius:10px;background:var(--panel-2);color:var(--text-2);font-size:10px;padding:0 10px}.spai-head button{cursor:pointer;font-weight:800}.spai-head button.active{border-color:var(--brand-line);color:var(--brand-2);background:var(--brand-soft)}
    .spai-memory-bar{display:flex;justify-content:space-between;gap:12px;align-items:center;padding:10px 20px;background:rgba(217,173,89,.04);border-bottom:1px solid var(--line);font-size:9px;color:var(--muted)}.spai-memory-bar b{color:var(--text-2)}.spai-memory-bar span:last-child{text-align:right}
    .spai-log{display:flex;flex-direction:column;gap:12px;min-height:250px;max-height:520px;overflow:auto;padding:18px 20px;scroll-behavior:smooth}.spai-msg{display:grid;grid-template-columns:32px minmax(0,1fr);gap:10px;max-width:92%}.spai-msg.user{align-self:flex-end;grid-template-columns:minmax(0,1fr) 32px}.spai-msg .spai-avatar{display:grid;place-items:center;width:32px;height:32px;border:1px solid var(--line);border-radius:10px;background:var(--panel-2);font-size:8px;font-weight:900;color:var(--brand-2)}.spai-msg.user .spai-avatar{order:2;color:var(--text-2)}.spai-bubble{padding:13px 14px;border:1px solid var(--line);border-radius:14px;background:var(--panel-2);font-size:11px;line-height:1.65;color:var(--text-2);unicode-bidi:plaintext}.spai-msg.user .spai-bubble{background:var(--brand-soft);border-color:var(--brand-line)}.spai-bubble b{display:block;margin-bottom:4px;color:var(--text);font-size:9px;letter-spacing:.04em}.spai-bubble p{margin:0;white-space:pre-wrap;unicode-bidi:plaintext}.spai-bubble.typing p:after{content:' ···';animation:spaiBlink 1s infinite}@keyframes spaiBlink{50%{opacity:.25}}
    .spai-quick{display:flex;gap:7px;overflow:auto;padding:0 20px 12px}.spai-quick button{flex:0 0 auto;padding:8px 11px;border:1px solid var(--line);border-radius:999px;background:transparent;color:var(--muted);font-size:9px;cursor:pointer}.spai-quick button:hover{color:var(--brand-2);border-color:var(--brand-line)}
    .spai-composer{display:grid;grid-template-columns:auto minmax(0,1fr) auto;gap:9px;align-items:end;padding:14px 20px 12px;border-top:1px solid var(--line);background:color-mix(in srgb,var(--panel-2) 78%,transparent)}.spai-composer textarea{width:100%;min-height:48px;max-height:150px;resize:none;border:1px solid var(--line);border-radius:12px;background:var(--panel);color:var(--text);padding:12px 13px;font:inherit;font-size:11px;line-height:1.5;unicode-bidi:plaintext}.spai-composer textarea:focus{outline:none;border-color:var(--brand-line);box-shadow:0 0 0 3px rgba(217,173,89,.07)}.spai-round{width:44px;height:44px;border:1px solid var(--line);border-radius:12px;background:var(--panel);color:var(--text-2);font-size:17px;cursor:pointer}.spai-round.listening{border-color:#64d7a1;color:#64d7a1;box-shadow:0 0 0 4px rgba(100,215,161,.08)}.spai-send{width:48px;height:44px;border:0;border-radius:12px;background:var(--brand);color:#111;font-size:18px;font-weight:900;cursor:pointer}.spai-send:disabled,.spai-round:disabled{opacity:.45;cursor:not-allowed}
    .spai-status{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:0 20px 14px;color:var(--muted-2);font-size:8px}.spai-status [data-spai-source]{color:var(--brand-2);font-weight:800;text-align:right}.spai-task-label{margin-top:24px;padding-top:6px;border-top:1px solid var(--line)}.spai-task-label b{display:block;margin-top:14px;font-size:12px}.spai-task-label span{color:var(--muted);font-size:9px}
    @media(max-width:760px){.spai-head{align-items:flex-start;flex-direction:column}.spai-head-actions{width:100%;justify-content:flex-start}.spai-head select{max-width:44%;min-width:0}.spai-msg{max-width:98%}.spai-log{max-height:58vh}.spai-memory-bar{align-items:flex-start;flex-direction:column}.spai-memory-bar span:last-child{text-align:left}.spai-composer{padding-left:12px;padding-right:12px}.spai-round{width:42px}.spai-status{padding-left:12px;padding-right:12px}}
    `;document.head.appendChild(s);
  }

  function languageValue(){return $('#spai-language')?.value||localStorage.getItem('superpro_workspace_language')||'auto'}
  function voicePreference(){return $('#spai-voice-style')?.value||localStorage.getItem('superpro_workspace_voice_style')||'auto'}
  function detectLanguage(text){const chosen=languageValue();if(chosen!=='auto')return chosen;return window.GDSProductGuide?.detectLanguage?.(text)||'en'}
  function speechLocale(lang){return localeMap[lang]||'en-AU'}
  function refreshVoices(){voices=speechSupported?(speechSynthesis.getVoices()||[]):[];return voices}
  function findVoice(locale){
    refreshVoices();const family=locale.split('-')[0].toLowerCase();const exact=voices.filter(v=>String(v.lang||'').toLowerCase()===locale.toLowerCase());const same=voices.filter(v=>String(v.lang||'').toLowerCase().split('-')[0]===family);const pool=exact.length?exact:same;if(!pool.length)return null;
    const pref=voicePreference();if(pref==='female'){const v=pool.find(x=>femaleHints.test(x.name));if(v)return v}if(pref==='male'){const v=pool.find(x=>maleHints.test(x.name));if(v)return v}return pool.find(v=>v.default)||pool[0]
  }
  function splitSpeech(text,max=220){
    const cleaned=String(text||'').replace(/\s+/g,' ').trim();if(!cleaned)return[];const sentences=cleaned.match(/[^.!?؟۔]+[.!?؟۔]?/g)||[cleaned],chunks=[];let cur='';
    for(const sentence of sentences){const s=sentence.trim();if(!s)continue;if((cur+' '+s).trim().length<=max){cur=(cur+' '+s).trim();continue}if(cur)chunks.push(cur);if(s.length<=max){cur=s;continue}for(const word of s.split(/\s+/)){if((cur+' '+word).trim().length>max&&cur){chunks.push(cur);cur=word}else cur=(cur+' '+word).trim()}}if(cur)chunks.push(cur);return chunks
  }
  function setStatus(text,source=''){const s=$('[data-spai-status]');if(s)s.textContent=text;const src=$('[data-spai-source]');if(src&&source)src.textContent=source}
  function stopSpeech(){speechRun++;if(heartbeat){clearInterval(heartbeat);heartbeat=null}if(serverAudio){try{serverAudio.pause();serverAudio.src=''}catch{}serverAudio=null}if(serverAudioUrl){try{URL.revokeObjectURL(serverAudioUrl)}catch{}serverAudioUrl=''}if(speechSupported){speechSynthesis.cancel();speechSynthesis.resume?.()}}
  async function speakBrowser(text,lang){
    if(!speechSupported)return false;refreshVoices();const locale=speechLocale(lang),voice=findVoice(locale),chunks=splitSpeech(text);if(!chunks.length)return false;const run=++speechRun;let i=0;
    const next=()=>{if(run!==speechRun||!voiceReplies)return;if(i>=chunks.length){if(heartbeat){clearInterval(heartbeat);heartbeat=null}setStatus('Voice reply finished. Ask another question whenever you are ready.','Device voice');return}const u=new SpeechSynthesisUtterance(chunks[i++]);u.lang=voice?.lang||locale;if(voice)u.voice=voice;u.rate=.96;u.pitch=1;u.onstart=()=>setStatus(`Speaking in ${u.lang}${voice?` · ${voice.name}`:' · device speech service'}`,'Device voice fallback');u.onend=next;u.onerror=e=>{const r=String(e?.error||'').toLowerCase();if(!['canceled','interrupted'].includes(r))setStatus(`Voice playback could not continue in ${locale}. Text remains available.`,'Voice limited')};try{speechSynthesis.resume?.();speechSynthesis.speak(u)}catch{return false}};
    heartbeat=setInterval(()=>{if(run===speechRun&&voiceReplies&&speechSynthesis.paused)speechSynthesis.resume?.()},5000);next();return true
  }
  async function speak(text,lang=lastLanguage){
    if(!voiceReplies)return;stopSpeech();
    try{const response=await fetch('/api/saas/voice/speech',{method:'POST',credentials:'same-origin',headers:{'content-type':'application/json'},body:JSON.stringify({text,language:lang,voice:voicePreference()})});if(response.ok){const blob=await response.blob();serverAudioUrl=URL.createObjectURL(blob);serverAudio=new Audio(serverAudioUrl);serverAudio.onplay=()=>setStatus(`Speaking naturally in ${speechLocale(lang)} using server AI voice.`,'AI voice');serverAudio.onended=()=>{if(serverAudioUrl)URL.revokeObjectURL(serverAudioUrl);serverAudioUrl='';serverAudio=null;setStatus('Voice reply finished. Ask another question whenever you are ready.','AI voice')};serverAudio.onerror=()=>speakBrowser(text,lang);await serverAudio.play();return}}catch{}
    await speakBrowser(text,lang)
  }
  async function autoListen(){
    if(!navigator.mediaDevices?.getUserMedia||!window.MediaRecorder){setStatus('Automatic language microphone mode is not supported in this browser. Choose a language or type your question.','Voice input unavailable');return}
    if(mediaRecorder&&mediaRecorder.state==='recording'){mediaRecorder.stop();return}
    stopSpeech();let chunks=[];try{mediaStream=await navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true}});const preferred=['audio/webm;codecs=opus','audio/webm'].find(t=>MediaRecorder.isTypeSupported?.(t))||'';mediaRecorder=new MediaRecorder(mediaStream,preferred?{mimeType:preferred}:undefined);mediaRecorder.ondataavailable=e=>{if(e.data?.size)chunks.push(e.data)};mediaRecorder.onstart=()=>{listening=true;$('#spai-mic')?.classList.add('listening');setStatus('Listening with automatic language detection… speak naturally.','AI transcription')};mediaRecorder.onstop=async()=>{listening=false;$('#spai-mic')?.classList.remove('listening');mediaStream?.getTracks().forEach(t=>t.stop());const blob=new Blob(chunks,{type:mediaRecorder.mimeType||'audio/webm'});if(!blob.size)return setStatus('No speech was captured. Try again.','Voice input');const fd=new FormData();fd.append('audio',blob,'speech.webm');fd.append('language','auto');setStatus('Detecting language and transcribing…','AI transcription');try{const r=await fetch('/api/saas/voice/transcribe',{method:'POST',credentials:'same-origin',body:fd});const d=await r.json();if(!r.ok)throw new Error(d.error||'Transcription failed.');const input=$('#spai-input');input.value=d.text;lastLanguage=d.language||detectLanguage(d.text);resize();setStatus(`Detected ${lastLanguage.toUpperCase()} · transcription ready.`,'AI transcription');await ask(d.text)}catch(e){setStatus(e.message||'Voice transcription failed.','Voice input')}};mediaRecorder.start();setTimeout(()=>{if(mediaRecorder?.state==='recording')mediaRecorder.stop()},15000)}catch(e){mediaStream?.getTracks().forEach(t=>t.stop());setStatus(e?.name==='NotAllowedError'?'Microphone permission is blocked. Allow microphone access and try again.':'Microphone could not start.','Voice input')}
  }

  function addMessage(role,text,label=''){
    const log=$('#spai-log');if(!log)return null;const el=document.createElement('div');el.className=`spai-msg ${role}`;const avatar=`<span class="spai-avatar">${role==='user'?'YOU':'AI'}</span>`;const bubble=`<div class="spai-bubble" dir="auto">${label?`<b>${esc(label)}</b>`:''}<p dir="auto">${esc(text)}</p></div>`;el.innerHTML=role==='user'?bubble+avatar:avatar+bubble;log.append(el);log.scrollTop=log.scrollHeight;return el
  }
  function setQuick(items=[]){const box=$('#spai-quick');if(!box)return;box.innerHTML=items.slice(0,4).map(x=>`<button type="button" dir="auto">${esc(x)}</button>`).join('');box.querySelectorAll('button').forEach(b=>b.onclick=()=>ask(b.textContent))}
  function resize(){const t=$('#spai-input');if(!t)return;t.style.height='auto';t.style.height=Math.min(150,t.scrollHeight)+'px'}
  function restoreConversation(){
    const memory=window.GDSProductGuide?.memory?.load?.('workspace')||[];const recent=memory.slice(-6);if(!recent.length)return;
    for(const x of recent){addMessage('user',x.q);addMessage('ai',x.a,'Remembered guidance')}setStatus(`Restored ${recent.length} recent conversation turn${recent.length===1?'':'s'} from expanded workspace memory.`,'Memory active')
  }
  async function ask(question){
    const q=String(question||'').trim();if(!q)return;const input=$('#spai-input'),send=$('#spai-send');lastLanguage=detectLanguage(q);addMessage('user',q);input.value='';resize();send.disabled=true;const waiting=addMessage('ai','Thinking with product knowledge and conversation context…','Super Pro AI');waiting?.querySelector('.spai-bubble')?.classList.add('typing');setStatus('Reviewing the relevant product, workspace and conversation context…','Thinking');
    try{
      const guide=window.GDSProductGuide;if(!guide?.answerAsync)throw new Error('AI guide is not ready.');const result=await guide.answerAsync(q,'workspace',lastTopic,languageValue());lastTopic=result.topic||lastTopic;lastLanguage=result.language||lastLanguage;waiting?.remove();addMessage('ai',result.text,'Super Pro AI');setQuick(result.suggestions||[]);const source=result.source==='configured-ai-provider'?'Connected AI + product memory':result.source==='expanded-product-memory'?'Expanded product memory':'Product guidance';setStatus('Answer ready. Ask a follow-up and the assistant will use the conversation context.',source);paintProviderStatus(result.source);await speak(result.text,lastLanguage)
    }catch(err){waiting?.remove();addMessage('ai','I could not complete that answer. Please try the question again; your previous conversation remains available.','Super Pro AI');setStatus(err.message||'The AI request could not be completed.','Retry available')}finally{send.disabled=false;input.focus()}
  }
  function paintProviderStatus(source){
    const pill=$('#assistant-view .page-heading .status-pill');if(!pill)return;
    if(source==='configured-ai-provider'){pill.className='status-pill good';pill.innerHTML='<span></span> AI provider connected'}else{pill.className='status-pill neutral';pill.innerHTML='<span></span> Expanded product memory active'}
  }

  function initRecognition(){
    if(!Recognition)return null;const r=new Recognition();r.interimResults=true;r.continuous=false;r.lang=speechLocale(languageValue()==='auto'?lastLanguage:languageValue());
    r.onstart=()=>{listening=true;$('#spai-mic')?.classList.add('listening');setStatus(`Listening in ${r.lang}… speak naturally.`,'Voice input')};
    r.onresult=e=>{let text='';for(let i=e.resultIndex;i<e.results.length;i++)text+=e.results[i][0].transcript;const input=$('#spai-input');input.value=text;input.dir='auto';resize();const last=e.results[e.results.length-1];if(last?.isFinal)setTimeout(()=>ask(input.value),180)};
    r.onerror=e=>setStatus(e.error==='not-allowed'?'Microphone permission is blocked. Allow microphone access or type your question.':'Voice recognition could not start. Try again or type your question.','Voice input');
    r.onend=()=>{listening=false;$('#spai-mic')?.classList.remove('listening')};return r
  }

  function install(){
    const view=$('#assistant-view');if(!view||$('#spai-v16'))return false;installStyle();
    const panel=document.createElement('article');panel.id='spai-v16';panel.className='spai-v16';panel.innerHTML=`
      <div class="spai-head"><div><h2>Super Pro AI Workspace Guide</h2><p>Ask about the software, your workspace, setup, connections, navigation or an operational workflow. Answers stay concise and approval-aware.</p></div><div class="spai-head-actions"><select id="spai-language" aria-label="Reply language"><option value="auto">Auto language</option><option value="en">English</option><option value="ur">Urdu</option><option value="hi">Hindi</option><option value="pa">Punjabi</option><option value="ar">Arabic</option><option value="bn">Bengali</option><option value="ta">Tamil</option><option value="zh">Chinese</option><option value="ja">Japanese</option><option value="ko">Korean</option><option value="es">Spanish</option><option value="fr">French</option></select><select id="spai-voice-style" aria-label="Voice preference"><option value="auto">Voice: Auto</option><option value="female">Voice: Female</option><option value="male">Voice: Male</option></select><button id="spai-speaker" type="button" class="active" aria-pressed="true" title="Toggle spoken replies">🔊</button><button id="spai-clear" type="button" title="Clear this device conversation memory">Clear</button></div></div>
      <div class="spai-memory-bar"><span><b>Expanded memory</b> · up to 500 conversation turns on this device, with relevant older context recalled automatically.</span><span>Product knowledge + current workspace context · secrets are never included.</span></div>
      <div class="spai-log" id="spai-log" aria-live="polite"><div class="spai-msg ai"><span class="spai-avatar">AI</span><div class="spai-bubble" dir="auto"><b>Super Pro AI</b><p dir="auto">Ask me what you want to do. I can explain features, guide setup and connections, answer follow-up questions, or help you work through the current workspace.</p></div></div></div>
      <div class="spai-quick" id="spai-quick"><button type="button">How do I use this software?</button><button type="button">How do I connect WhatsApp?</button><button type="button">How do I add an employee?</button><button type="button">What still needs setup?</button></div>
      <form class="spai-composer" id="spai-form"><button class="spai-round" id="spai-mic" type="button" aria-label="Ask by voice">◉</button><textarea id="spai-input" rows="1" dir="auto" placeholder="Ask any Super Pro AI Office Manager question…" aria-label="Ask Super Pro AI"></textarea><button class="spai-send" id="spai-send" type="submit" aria-label="Send question">➜</button></form>
      <div class="spai-status"><span data-spai-status>Ready for text or voice. Select a language for best microphone accuracy.</span><span data-spai-source>Expanded memory ready</span></div>`;
    const strip=view.querySelector('.ai-command-strip');if(strip)strip.insertAdjacentElement('afterend',panel);else view.querySelector('.page-heading')?.insertAdjacentElement('afterend',panel);
    const originalForm=$('#assistant-form');if(originalForm&&!originalForm.previousElementSibling?.classList.contains('spai-task-label'))originalForm.insertAdjacentHTML('beforebegin','<div class="spai-task-label"><b>Saved operational tasks</b><span>Use this separate form when you want to save a durable operational instruction into the workspace task history.</span></div>');
    const pill=view.querySelector('.page-heading .status-pill');if(pill){pill.className='status-pill neutral';pill.innerHTML='<span></span> AI guide ready'}

    const language=$('#spai-language'),voice=$('#spai-voice-style');const savedLang=localStorage.getItem('superpro_workspace_language');if(savedLang&&[...language.options].some(o=>o.value===savedLang))language.value=savedLang;const savedVoice=localStorage.getItem('superpro_workspace_voice_style');if(savedVoice&&[...voice.options].some(o=>o.value===savedVoice))voice.value=savedVoice;
    language.onchange=()=>{localStorage.setItem('superpro_workspace_language',language.value);lastLanguage=language.value==='auto'?'en':language.value;if(recognition)recognition.lang=speechLocale(lastLanguage);setStatus(language.value==='auto'?'Auto language is active. For microphone accuracy, choose the language you plan to speak.':`Text replies and microphone input are set to ${language.selectedOptions[0].textContent}.`,'Language ready')};
    voice.onchange=()=>{localStorage.setItem('superpro_workspace_voice_style',voice.value);setStatus(`Voice preference: ${voice.selectedOptions[0].textContent.replace('Voice: ','')}. Language matching takes priority.`,'Voice ready')};
    $('#spai-speaker').onclick=()=>{voiceReplies=!voiceReplies;localStorage.setItem('superpro_workspace_voice',voiceReplies?'on':'off');const b=$('#spai-speaker');b.textContent=voiceReplies?'🔊':'🔇';b.classList.toggle('active',voiceReplies);b.setAttribute('aria-pressed',String(voiceReplies));if(!voiceReplies){stopSpeech();setStatus('Spoken replies are off. Text answers remain active.','Voice off')}else setStatus('Spoken replies are on. Server AI voice is preferred; the device voice is used only as a fallback.','Voice on')};
    $('#spai-clear').onclick=()=>{stopSpeech();window.GDSProductGuide?.memory?.clear?.('workspace');$('#spai-log').innerHTML='<div class="spai-msg ai"><span class="spai-avatar">AI</span><div class="spai-bubble" dir="auto"><b>Fresh conversation</b><p dir="auto">Conversation memory on this device has been cleared. Ask a new Super Pro question whenever you are ready.</p></div></div>';lastTopic='';setStatus('Fresh conversation ready.','Memory cleared')};
    $('#spai-form').onsubmit=e=>{e.preventDefault();ask($('#spai-input').value)};$('#spai-input').addEventListener('input',resize);$('#spai-input').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();$('#spai-form').requestSubmit()}});$('#spai-quick').querySelectorAll('button').forEach(b=>b.onclick=()=>ask(b.textContent));
    recognition=initRecognition();$('#spai-mic').onclick=()=>{stopSpeech();if(languageValue()==='auto'){autoListen();return}if(!recognition){setStatus('This browser does not expose speech recognition. Automatic server transcription may still be available; choose Auto language or type your question.','Voice input unavailable');return}if(listening)recognition.stop();else{recognition.lang=speechLocale(languageValue());try{recognition.start()}catch{setStatus('Microphone recognition is already starting.','Voice input')}}};
    if(speechSupported){refreshVoices();speechSynthesis.addEventListener?.('voiceschanged',refreshVoices)}else{$('#spai-speaker').disabled=true;setStatus('This browser does not expose spoken reply playback. Text and supported microphone input remain available.','Text mode')}
    restoreConversation();window.dispatchEvent(new CustomEvent('superpro:ai-operations-v16-ready',{detail:{version:VERSION}}));return true
  }

  let tries=0;const timer=setInterval(()=>{tries++;if(install()||tries>80)clearInterval(timer)},100);
})();